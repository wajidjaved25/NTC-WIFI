# Syslog Server Configuration
# Copy this to .env and update with your values

# Main Server Connection (WiFi Portal)
MAIN_SERVER_IP=10.2.49.27
MAIN_DB_PORT=5432
MAIN_DB_NAME=ntc_wifi_admin
MAIN_DB_USER=ntc_admin
MAIN_DB_PASSWORD=NTCWifi2024!

# Local Database (Firewall Logs)
LOGS_DB_HOST=localhost
LOGS_DB_PORT=5432
LOGS_DB_NAME=ntc_wifi_logs
LOGS_DB_USER=syslog_user
LOGS_DB_PASSWORD=SecureLogPassword123

# Correlation Settings
CORRELATION_INTERVAL=300        # Seconds between correlation runs (5 minutes)
LOOKBACK_MINUTES=30            # How far back to look for sessions

# Syslog Receiver Settings (in syslog_receiver.py)
SYSLOG_HOST=0.0.0.0
SYSLOG_PORT=514
QUEUE_SIZE=20000
BATCH_SIZE=2000
BATCH_TIMEOUT=0.5
NUM_WORKERS=4
